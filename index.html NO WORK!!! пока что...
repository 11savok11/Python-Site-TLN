<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Longest Night</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <div class="container">
        <h1>The Longrest Night</h1>
        {% if students %}
            <div class="students-list">
                {% for student in students %}
                    <div class="student-item">
                        <span class="">{{ loop.index }}</span>
                        <span class="">{{  }}</span>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="no-students"></p>
        {% endif %}

        <div class="footer">
            <p> {{}}</p>
        </div>
    </div>
       <div class="navigation">
            <a href="{{ url_for('Discord') }}" class="btn">Наш Discord</a>
            <a href="{{ url_for('YouTube') }}" class="btn">YouTube</a>
           <a href="{{ url_for('Magazine') }}" class="btn">Магазин</a>
    </div>
</body>
</html>
